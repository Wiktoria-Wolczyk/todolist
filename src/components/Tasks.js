export const Tasks = () => {
  return (
    <div 
      className="TodosList"
      style={{
        backgroundColor: "green",
        width: "100%",
        height: '100%',
        display: "flex",   
      }}
    >
      Today's tasks
    </div>
  )
}